import Sample from "../samples/basic";
import { Message } from "../../Message";

# Live

<Message variant="warning">

**EXPERIMENTAL** This component is still under active development and not
available yet as a Timvir Block.

</Message>

The `<Live>` component is a wrapper around [react-live].

<Sample />

[react-live]: https://github.com/FormidableLabs/react-live
